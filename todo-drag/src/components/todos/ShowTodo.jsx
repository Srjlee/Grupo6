import { useSelector } from "react-redux";

const ShowTodo = () => {
  const { task: title, description } = useSelector((state) => state.todo);  
  return (
    <div
      className="modal fade"
      id="modalScreen"
      tabIndex="-1"
      aria-labelledby="modalScreenLabel"
      aria-hidden="true"
    >
      <div className="modal-dialog">
        <div className="modal-content">
          <div className="modal-header">
            <h5 className="modal-title" id="modalScreenLabel">
              {title}
            </h5>
            <button
              type="button"
              className="btn-close"
              data-bs-dismiss="modal"
              aria-label="Close"
            ></button>
          </div>
          <div className="modal-body">{description}</div>
          <div className="modal-footer">
            <button
              type="button"
              className="btn btn-secondary"
              data-bs-dismiss="modal"
            >
              Cerrar
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

export default ShowTodo;
